<%= form_for @entries[date.to_s] || Entry.new do |form| %>
  <div class='form-group'>
    <%= form.text_area :body, class: 'form-control', rows: '3' %>
  </div>
  <div class='container text-right'>
    <%= form.submit %>
  </div>
<% end %>
